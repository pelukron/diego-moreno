# Tomorrow’s human trusts Artifacts, not the session

The next iteration of this site (credentials, portrait, copy) is only trustworthy if today’s rules still fail a PR without re-asking Grok.

Prefer a new Gate check or a line in `scripts/domain-rules.json` over a new agent, Biome stack, or a longer chat. CONTEXT.md and ADRs hold meaning; scripts hold enforcement. The session log is not a source of truth.

Rejected: remembering quality in AGENTS.md instead of a script, formatter-as-quality, “we’ll be careful next time”.
